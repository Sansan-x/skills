# 审计报告模板

在阶段7中使用此模板构建最终安全审计报告。将各前序阶段的发现填入对应章节。

---

## 报告输出规范

**输出目录：** `./reports/`
**文件命名：** `[项目名称]-goaudit-[YYYYMMDD-HHMM].md`
**示例：** `./reports/myproject-goaudit-20260319-1430.md`

在生成报告前，先创建输出目录：
```bash
mkdir -p ./reports
```

项目名称获取规则：
1. 优先从 `go.mod` 的 module 声明中提取最后一个路径段
2. 如果不可用，使用项目根目录名
3. 移除名称中的特殊字符，仅保留字母、数字、连字符和下划线

时间使用审计执行时的当前时间，格式为 `YYYYMMDD-HHMM`。

---

## 报告头

```markdown
# Go代码安全审计报告：[项目名称]

**审计日期：** [YYYY-MM-DD]
**审计执行：** Claude（AI辅助代码审计）
**审计模式：** [快速扫描 / 深度审计]
**审计领域：** [通用领域 / 云核领域]
**报告版本：** 1.0
```

---

## 第1章：执行摘要

面向技术负责人和安全利益相关者的简明概述。

```markdown
## 1. 执行摘要

### 整体风险评估：[严重 / 高危 / 中危 / 低危]

本次对 [项目名称] 的安全审计共发现 **[总数]** 个安全问题：

| 严重性 | 数量 |
|--------|------|
| 致命   | [N]  |
| 严重   | [N]  |
| 一般   | [N]  |
| 提示   | [N]  |

### 关键发现

1. **[最严重发现标题]** — [一句话描述和影响]
2. **[第二严重发现]** — [一句话描述和影响]
3. **[第三严重发现]** — [一句话描述和影响]

### 攻击链识别

识别出 [N] 条攻击链，将多个漏洞组合为更高影响的利用路径。
最严重的攻击链：[最高影响链的简要描述]。

### 首要修复建议

1. [最高优先级修复 — 做什么及为什么]
2. [第二优先级修复]
3. [第三优先级修复]
```

---

## 第2章：项目概况

汇总阶段1的项目背景分析。

```markdown
## 2. 项目概况

### 项目类型
[例：REST API服务、gRPC微服务、CLI工具]

### 技术栈

| 组件     | 技术 |
|----------|------|
| 语言     | Go [版本] |
| 框架     | [gin/echo/chi/...] |
| 数据库   | [postgres/mysql/...] 通过 [gorm/sqlx/...] |
| 认证     | [JWT/OAuth2/会话/...] |
| 其他     | [重要依赖] |

### 架构概述
[项目架构、关键模块、信任边界的简要描述]

### 关键模块

| 模块 | 说明 | 安全关联 |
|------|------|----------|
| [模块名] | [功能] | [安全重要性] |
| ... | ... | ... |
```

---

## 第3章：审计范围与方法

记录审计内容和方法。

```markdown
## 3. 审计范围与方法

### 范围

**包含：**
- [审计的目录/模块列表（模块级）]
- [覆盖的具体功能区域]

**排除：**
- [生成代码、vendor依赖、测试文件等]
- [每项排除的原因]

约束：本章节只保留模块/目录级范围摘要，不输出冗长的逐文件清单（与附录 A.1 的统计表区分）；不展示模式库来源/模式库路径/已加载模式数或类别。

### 方法论

**模式：** [深度审计 / 快速扫描]
**领域：** [通用领域 / 云核领域]

**执行的审计阶段：**
1. 项目背景分析
2. 基于 [项目类型] 特征的审计策略设计
3. 基于Go漏洞模式库的LLM漏洞发现
4. [如深度审计] 对每个发现进行数据流追踪和污点分析
5. [如深度审计] 误报验证
6. 漏洞分类和CVSS对齐的严重性评级
7. [如深度审计] 攻击链组合分析

```

---

## 第4章：发现汇总

所有发现的概览表格。

```markdown
## 4. 发现汇总

| ID | 标题 | 严重性 | CWE | 位置 | 置信度 |
|----|------|--------|-----|------|--------|
| VULN-001 | [标题] | 致命 | CWE-89 | `pkg/handler/user.go:45` | 确认 |
| VULN-002 | [标题] | 严重 | CWE-287 | `pkg/auth/jwt.go:23` | 确认 |
| VULN-003 | [标题] | 一般 | CWE-200 | `pkg/middleware/error.go:12` | 可能 |
| ... | ... | ... | ... | ... | ... |

### 严重性分布

- **致命：** [N] 个发现，需要立即修复
- **严重：** [N] 个发现，需要尽快关注
- **一般：** [N] 个发现，短期内处理
- **提示：** [N] 个发现，合适时处理
```

---

## 第5章：详细发现

**发现汇总表（第4章）中的所有漏洞必须全部输出在此章节中。** 按等级排列：致命 → 严重 → 一般 → 提示。每个漏洞包含完整的6个必填子章节，缺一不可。

篇幅受限时的处理：按等级分批输出（先致命全部、再严重全部、依次一般和提示），如果单次输出被截断则接续输出剩余漏洞追加到报告中，确保最终报告包含所有漏洞。禁止省略、合并或简化任何漏洞。

```markdown
## 5. 详细发现

[按等级从高到低，逐一输出第4章汇总表中的每一个漏洞。
 汇总表中有N个漏洞，此处就必须输出N个完整条目。不得省略任何一个。]

---

### VULN-[ID]：[描述性标题]

**严重性：** [致命 / 严重 / 一般 / 提示]
**CVSS评分：** [X.X]（[向量字符串（如适用）]）
**CWE：** CWE-[ID] — [CWE名称]
**置信度：** [确认 / 可能 / 存疑]
**位置：** `[文件]:[行范围]` 函数 `[函数名]`

#### 5.[N].1 漏洞描述

[对漏洞的清晰解释，必须回答以下问题：]
- 漏洞的本质是什么？（技术原理）
- 为什么此处代码存在安全风险？（根因分析）
- 如果被利用，安全影响是什么？（潜在后果）

#### 5.[N].2 漏洞代码

[展示包含漏洞的完整函数或代码段。代码片段必须：]
- 包含完整函数体（不仅仅是漏洞行）
- 标注文件路径和行号
- 文件路径、行号一定是真实存在的，切记臆想
- 使用 `// ↑ 漏洞:` 注释标记漏洞行

```go
// 文件: [文件路径]
// 行: [起始]-[结束]

func [函数名]([参数]) [返回值] {
    [完整函数代码...]
    [漏洞代码行]
    // ↑ 漏洞: [简要说明漏洞原因]
    [函数剩余代码...]
}
```

#### 5.[N].3 数据流路径

[使用精简结构化字段展示数据流证据与关键链路。此章节不可省略。]

**数据流证据来源：**

- **ChainEvidenceType：** `[ToolConfirmed / LLMInferred]`
- **ToolName：** `[codebadger / n/a]`
- **ToolCallStatus：** `[ok / timeout / error / empty]`
- **ToolQuerySummary：** `[sink/parent/max_depth 摘要]`
- **ConfidenceCapReason：** `[原因 / n/a]`

说明：
- 若 `ChainEvidenceType=LLMInferred`，必须给出降级原因，且不能伪装为工具确认链路。
- 若 `ToolCallStatus in [timeout, error, empty]`，应在 `ConfidenceCapReason` 说明原因。

**数据链信息：**

- **漏洞ID：** `VULN-[ID]`
- **Source：** `[file:line @ function | 输入类型 | 表达式]`
- **Sink：** `[file:line @ function | 危险API | 表达式]`
- **关键传播路径：** `[入口函数 -> 关键中间节点(1..N) -> Sink]`

#### 5.[N].4 利用场景

[攻击者如何利用此漏洞的逐步描述，必须包含可直接复现的PoC。]

**攻击步骤：**

1. [步骤1：攻击者的具体操作]
2. [步骤2：下一步操作]
3. [步骤3：达成利用]

**PoC（概念验证）：**

[必须提供以下至少一种形式的具体PoC，能让安全人员直接复现漏洞：]

```bash
# HTTP接口漏洞的PoC示例 — curl命令
curl -X POST 'http://target:8080/api/endpoint' \
  -H 'Content-Type: application/json' \
  -d '{"field": "[恶意载荷]"}'
```

```go
// 或：Go代码形式的PoC
func exploit() {
    // [可直接运行的利用代码]
}
```

```python
# 或：Python脚本形式的PoC
import requests
resp = requests.post("http://target:8080/api/endpoint",
    json={"field": "[恶意载荷]"})
print(resp.text)
```

**预期结果：** [利用成功后攻击者观察到的结果]

#### 5.[N].5 影响

- **机密性：** [无 / 低 / 高] — [具体说明：可泄露什么数据]
- **完整性：** [无 / 低 / 高] — [具体说明：可篡改什么数据或行为]
- **可用性：** [无 / 低 / 高] — [具体说明：可导致什么服务中断]

#### 5.[N].6 修复建议

[给出具体、可操作且简明的修复建议。]

**修复建议（简明）：**

1. [建议1]

**修复说明：**

[解释为何这些修改可以消除漏洞或显著降低风险；必要时说明残余风险与适用边界]

**实现要点（可选）：**
- [关键改动点1]
- [关键改动点2]

---

[继续输出下一个漏洞的完整详情。重复以上模板，直到所有漏洞全部输出完毕。]

---

### 详细发现完整性校验

[在所有漏洞输出完毕后，附上此校验表。]

| 校验项 | 结果 |
|--------|------|
| 第4章汇总表漏洞总数 | [N] |
| 第5章已输出漏洞条目数 | [M] |
| 是否一致（N = M） | [是/否] |
| 各等级明细 | 致命[x]个 + 严重[x]个 + 一般[x]个 + 提示[x]个 = [M] |
| 遗漏的漏洞ID | [无 / 列出遗漏ID] |

[如果 N ≠ M，必须继续输出遗漏的漏洞条目直到 M = N。]
```

---

## 第6章：攻击链分析

展示阶段6识别的攻击链。

```markdown
## 6. 攻击链分析

### 攻击链1：[描述性名称]

**组合严重性：** [严重 / 高危 / 中危]
**涉及漏洞：** VULN-[ID1], VULN-[ID2], VULN-[ID3]

**攻击叙述：**

| 步骤 | 漏洞 | 攻击动作 | 攻击者获得 |
|------|------|----------|-----------|
| 1 | VULN-[ID1]：[标题] | [攻击者做什么] | [获得的能力] |
| 2 | VULN-[ID2]：[标题] | [利用步骤1的输出...] | [升级的能力] |
| 3 | VULN-[ID3]：[标题] | [利用步骤2的输出...] | [最终影响] |

**前置条件：** [攻击者开始前需要什么 — 如：网络访问、有效的低权限账户]

**最终影响：** [最终后果 — 如：完全数据库访问、以服务账户执行RCE、完全账户接管]

**链验证：** [确认每步的输出切实启用了下一步。引用具体代码路径和数据流。]

---

[其他链重复以上格式]

### 未识别到攻击链

[如果未发现有效链，明确说明：]

未识别到有效攻击链。发现的漏洞是独立的，不构成超出单个影响的有意义的链式利用。
```

---

## 第7章：附录

```markdown
## 7. 附录

### A. 审计覆盖统计

#### A.1 文件级（必输出）

与 `go-audit` 阶段 4 的 `CoverageBackfill` 文件维度一致；用于评估本次审计触达了多少 `.go` 文件。分母依赖审计策略中的排除项定义，若策略未约定排除规则或无法枚举，覆盖率标 `N/A` 或「不确定」，并在此说明原因。

| 指标 | 值 |
|------|----|
| 已审计唯一 `.go` 文件数（files_audited_unique） | [N] |
| 必审目录下符合排除规则的 `.go` 总数（files_in_must_audit_dirs） | [N 或 N/A] |
| 必审文件（glob）展开后的预期文件数（files_must_audit_globs_resolved） | [N 或 N/A] |
| 必审目录覆盖率（must_audit_dir_coverage） | [比例 或 N/A] |
| 全仓 `.go` 总数（按策略排除后，可选 repo_go_files_total） | [N 或 N/A] |

**可选（择一，用于追溯）：** 不强制全文罗列。需要时可附 **按包/目录聚合计数表**（例如每包已审文件数），或 **已审计文件路径列表**（仅路径，一行一个）。第 3 章正文仍保持模块级摘要，不展开冗长路径表。

#### A.2 模块/目录级

| 指标 | 值 |
|------|----|
| 模块总数 | [N] |
| 已覆盖模块数（covered） | [A] |
| 补扫覆盖模块数（backfilled） | [B] |
| 未覆盖模块数（uncovered） | [C] |
| 覆盖率 | [(A+B)/N] |

| 模块/目录 | 审计状态 | 备注 |
|-----------|----------|------|
| `sbi/*` | [covered/backfilled/uncovered] | [说明] |
| `nas|ngap|pfcp/*` | [covered/backfilled/uncovered] | [说明] |
| `context|session|state/*` | [covered/backfilled/uncovered] | [说明] |
| `gtp|tunnel/*` | [covered/backfilled/uncovered] | [说明] |
| `crypto|security/*` | [covered/backfilled/uncovered] | [说明] |
| `middleware|error|logging/*` | [covered/backfilled/uncovered] | [说明] |

#### A.3 模式执行覆盖（并行编排场景必输出）

用于证明“已加载的攻击模式已被实际执行”。当策略包含 `must_cover_categories/full_audit_categories` 时，本节必填。

| 类别 | patterns_loaded | patterns_executed | files_scanned | sink_hits | findings | unexecuted_reason |
|------|------------------|-------------------|---------------|-----------|----------|-------------------|
| FILE_OPS | [N] | [N] | [N] | [N] | [N] | [无/原因] |
| SQLI | [N] | [N] | [N] | [N] | [N] | [无/原因] |
| GO_RUNTIME | [N] | [N] | [N] | [N] | [N] | [无/原因] |
| ALL_REMAINING（可选聚合） | [N] | [N] | [N] | [N] | [N] | [无/原因] |

校验要求：
- `patterns_executed` 不得为空；若为 0 必须提供 `unexecuted_reason` 并在方法论说明补充约束原因。
- 必审高危类别（FILE_OPS/SQLI/GO_RUNTIME）若无 `sink_hits`，必须在 `unexecuted_reason` 中给出可复核证据（例如“全量扫描未命中指定 sink，附检索摘要”）。

### B. 排除的误报

| 发现 | 位置 | 排除原因 |
|------|------|----------|
| [描述] | `文件:行` | [在文件:行发现有效净化 — 参数化查询] |
| [描述] | `文件:行` | [路径不可达 — 端点需要管理员认证和内部网络] |
| ... | ... | ... |

### C. 方法论说明

**漏洞模式库信息：**

| 项目 | 详情 |
|------|------|
| 模式库来源 | [go-vuln-lib / 内置(references/vulnerability-patterns.md)] |
| 模式库路径 | [实际加载路径] |
| 已加载模式数 | [N] 个 |
| 已加载模式类别 | [列出加载的类别名称，如：SQL注入、命令注入、SSRF、...] |

- 使用的审计策略模板：[模板名称]
- 并行编排模式（如启用）：
  - `must_cover_categories`：[FILE_OPS, SQLI, GO_RUNTIME, ...]
  - `full_audit_categories`：[ALL_REMAINING 或明细列表]
  - Gate 结果摘要：[Gate-1/2/3/4 通过或失败原因]
  - Backfill 执行摘要：[按类别+目录补扫详情]
- [任何偏离标准方法论的情况及原因]
- [审计限制 — 未覆盖的区域及原因]
```

---

## 格式指南

1. **严重性标识** — 使用中文标签：严重、高危、中危、低危、信息

2. **发现完整性（最高优先级规则）** — 所有漏洞必须全部输出在主报告第5章"详细发现"中，一个不少。最终通过"详细发现完整性校验"表格确认总数一致。绝不允许用"以此类推"省略漏洞。篇幅受限时按等级分批输出，被截断时接续输出直到所有漏洞完整

3. **每个发现的6个必填子章节** — 对每个漏洞，以下6个子章节缺一不可：
   - 5.[N].1 漏洞描述：问题是什么、为什么危险、影响是什么
   - 5.[N].2 漏洞代码：包含完整函数体，标注文件路径、行号和漏洞行
   - 5.[N].3 数据流路径：精简结构化字段（证据来源 + 数据链信息）
   - 5.[N].4 利用场景：逐步攻击描述 + 可直接复现的PoC（curl/代码/脚本）
   - 5.[N].5 影响：机密性、完整性、可用性三维评估
   - 5.[N].6 修复建议：简明修复步骤 + 修复说明（可含实现要点）

4. **代码片段** — 始终包含文件路径和行号以便追溯。漏洞代码片段应包含完整函数体或足够上下文

5. **数据流路径** — 使用精简结构化字段（如第5章模板所示），最少包含：
   - 证据来源：`ChainEvidenceType`、`ToolName`、`ToolCallStatus`、`ToolQuerySummary`、`ConfidenceCapReason`
   - 数据链信息：`漏洞ID`、`Source`、`Sink`、`关键传播路径`

6. **利用PoC** — 每个发现的利用场景必须包含可直接复现的PoC，不能仅用文字描述。PoC形式包括但不限于：curl命令、HTTP原始请求、Python/Go利用脚本、gRPC调用示例

7. **修复建议可执行性** — 每个发现应提供可执行的修复步骤与修复说明；可给出关键实现要点

8. **发现ID** — 使用顺序 `VULN-001`、`VULN-002` 格式

9. **攻击链** — 使用编号步骤和清晰过渡；每步必须引用具体漏洞ID

10. **报告语言** — 全文使用中文，技术术语保持英文（如CWE、CVSS、SSRF等）

11. **报告长度** — 详尽但不冗余。每个发现应自包含，开发者仅凭报告即可修复
